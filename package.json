{
  "name": "vislow-biapp",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma low-code para geracao de visuais customizados do Power BI",
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "// comandos": "Cada um faz uma coisa POR INTEIRO. A ordem (build antes de lint pelos .d.ts, build antes de stage:vendor, stage:vendor antes do gate) vive no turbo.json, e vale igual aqui e no CI.",
    "dev": "turbo run dev",
    "build": "turbo run build stage:vendor",
    "verify": "turbo run build typecheck lint lint:docs test",
    "check": "turbo run build typecheck lint lint:docs test test:build",

    "typecheck": "turbo run typecheck",
    "test:build": "turbo run test:build",
    "stage:vendor": "turbo run stage:vendor",
    "dev:api": "turbo run dev --filter @vislow/api",
    "clean": "turbo run clean && rm -rf .turbo",

    "// tarefas de raiz": "Implementacoes de `//#lint`, `//#lint:docs` e `//#test` — o turbo as chama por este nome exato, entao elas nao podem ser wrappers do proprio turbo. Use `pnpm verify`, que as roda na ordem certa.",
    "lint": "eslint .",
    "lint:docs": "node scripts/check-docs.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/jsdom": "^28.0.3",
    "@types/node": "26.1.2",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "jsdom": "^30.0.1",
    "turbo": "2.10.7",
    "typescript": "5.9.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}