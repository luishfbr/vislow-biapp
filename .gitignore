node_modules/
dist/
.tmp/
coverage/
*.tsbuildinfo

# Cache do turbo: o `.turbo/` da raiz e os diretorios de log por pacote.
.turbo/

# Artefatos de build do Runtime Core — nunca versionados (secao 11 do doc de MVP)
apps/web/public/templates/*.pbiviz
packages/runtime/dist/

# Relatorio do analisador de bundle do webpack do pbiviz — regerado a cada build
webpack.statistics.prod.html

# Next.js
.next/
out/

# Permissoes e overrides locais do agente. As skills em .claude/skills/ e
# .agents/skills/ SAO versionadas — sao regra de projeto (secao 5 dos padroes).
.claude/settings.local.json

.DS_Store
*.log
.env
.env.local

# Pacotes internos compilados para dentro do template. Saida de
# `pnpm --filter @vislow/visual-template stage:vendor`.
packages/visual-template/vendor/

# Dependencias do template, instaladas uma vez por `stage:deps` e montadas por
# hardlink em cada build. Centenas de MB reproduziveis a partir do lockfile.
packages/visual-template/deps/
