{
  "name": "@vislow/visual-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scaffold estatico de um projeto pbiviz e o empacotamento dos pacotes internos que o visual gerado consome",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "template",
    "vendor"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "stage:vendor": "node scripts/stage-vendor.mjs",
    "clean": "rm -rf dist vendor .turbo"
  },
  "// devDependencies": "NAO sao importadas pelo codigo — nao remova por parecerem nao usadas. `scripts/stage-vendor.mjs` LE o `dist/` das duas e falha sem ele; declarar a aresta e o que faz o `^build` do turbo (e o `pnpm -r`) ordenarem certo, em vez de depender de quem roda os comandos lembrar da ordem. Ficam em devDependencies, e nao dependencies, porque o que chega ao visual e a COPIA em `vendor/`, nunca este node_modules — ver achado 39.",
  "devDependencies": {
    "@vislow/config-schema": "workspace:*",
    "@vislow/visual-kit": "workspace:*"
  }
}
